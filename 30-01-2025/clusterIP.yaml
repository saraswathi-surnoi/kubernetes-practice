kind: Service
apiVersion: v1
metadata:
  name: nginx
spec:
  selector:
    project: sarushop
    module: backend
    environment: dev
  ports:
  - name: nginx
    protocol: TCP
    port: 80 # service port
    targetPort: 80 # container port